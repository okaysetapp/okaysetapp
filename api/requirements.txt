# Minimal dependencies for the deployed Vercel Python function.
# Only top-level packages actually imported by server.py / google_lookup.py
# are listed here. Transitive deps (starlette, anyio, postgrest, etc.) are
# resolved automatically by pip from these.

fastapi==0.110.1
pydantic==2.12.5
email-validator==2.3.0
python-dotenv==1.2.1
supabase==2.27.2
slowapi==0.1.9
httpx==0.28.1
google-generativeai==0.8.6
